---
import UIString from "../UIString.astro";
import { getLanguageFromURL } from "../../util";
import MenuTemplate from "./MenuTemplate.astro";

export interface Props {
	editHref: string;
}

const { editHref } = Astro.props as Props;

const lang = getLanguageFromURL(Astro.url.pathname);

const linkItems = [
	{
		href: `/${lang}/contribute/`,
		text: "rightSidebar.contributorGuides",
		depth: 2,
		icon: 'open-book'
	},
	{
		href: editHref,
		text: "rightSidebar.editPage",
		depth: 2,
		icon:'pencil'
	}
]

---
<MenuTemplate headingTitle="rightSidebar.contribute" linkItems={linkItems} />
